version: '2'
services:
  web:
    build: .
    command: npm start
    volumes:
      - .:/usr/app/
      - /usr/app/node_modules
    ports:
      - "3000:3000"
    environment: 
      - http_proxy=${http_proxy}
      - https_proxy=${https_proxy}